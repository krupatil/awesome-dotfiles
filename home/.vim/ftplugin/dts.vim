runtime coding-common.vim

" DTS files are Linux files, so use adjusted conventions
setlocal tabstop=8
setlocal softtabstop=8
setlocal shiftwidth=8
exec "setlocal lcs=tab:\uB7\uB7,trail:\uB7,nbsp:~"
setlocal list
setlocal noexpandtab
